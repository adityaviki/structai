"""Transformation IR — model, ops, validate, lifecycle (plan §6).

The IR is the only thing the server executes. The agent emits it; the
interpreter consumes it. The generated `pipeline.py` is a viewable
artifact only (plan §6.5).
"""
