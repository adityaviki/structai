export const INSTRUCTION_SUGGESTIONS = [
  'Use snake_case for all column names',
  'Treat empty strings and "N/A" as NULL',
  'Convert all currencies to USD using order_date',
  'Skip rows with missing primary keys',
  'Keep existing table names — append rows if the table already exists',
]
